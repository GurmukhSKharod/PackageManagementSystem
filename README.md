# PackageManagementSystem
This project uses Java in IntelliJ to manage packages of 3 different types - Book, Perishable, Electronic. It uses IntelliJ's built-in GUI functionality on the front end to allow users to create new packages to be added to a list. All packages are stored both locally via JSON, and on a web server in the backend, using Spring Boot. 
